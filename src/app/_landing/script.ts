export const landingScript = "// ===================== THEME TOGGLE =====================\nconst root = document.documentElement;\nconst themeToggle = document.getElementById('themeToggle');\nconst iconSun = document.getElementById('iconSun');\nconst iconMoon = document.getElementById('iconMoon');\n\nfunction setTheme(theme) {\n  root.setAttribute('data-theme', theme);\n  themeToggle.setAttribute('aria-pressed', theme === 'light');\n  iconSun.style.display = theme === 'dark' ? 'block' : 'none';\n  iconMoon.style.display = theme === 'light' ? 'block' : 'none';\n}\nthemeToggle.addEventListener('click', () => {\n  const next = root.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';\n  setTheme(next);\n});\n\n// ===================== MOBILE MENU =====================\nconst burger = document.getElementById('burger');\nconst mobileMenu = document.getElementById('mobileMenu');\nburger.addEventListener('click', () => {\n  const isOpen = mobileMenu.classList.toggle('open');\n  burger.setAttribute('aria-expanded', isOpen);\n});\nmobileMenu.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {\n  mobileMenu.classList.remove('open');\n  burger.setAttribute('aria-expanded', false);\n}));\n\n// ===================== NAV BACKGROUND ON SCROLL =====================\nconst navBg = document.getElementById('navBg');\nconst scrollProgress = document.getElementById('scrollProgress');\nconst stickyCta = document.getElementById('stickyCta');\nconst heroSectionEl = document.querySelector('.hero');\nconst footerEl = document.querySelector('footer');\n\nwindow.addEventListener('scroll', () => {\n  navBg.classList.toggle('scrolled', window.scrollY > 40);\n\n  const docHeight = document.documentElement.scrollHeight - window.innerHeight;\n  const pct = docHeight > 0 ? (window.scrollY / docHeight) * 100 : 0;\n  scrollProgress.style.width = pct + '%';\n\n  const heroBottom = heroSectionEl.getBoundingClientRect().bottom;\n  const footerTop = footerEl.getBoundingClientRect().top;\n  const pastHero = heroBottom < 0;\n  const reachedFooter = footerTop < window.innerHeight;\n  stickyCta.classList.toggle('show', pastHero && !reachedFooter);\n}, { passive: true });\n\n// ===================== SCROLL REVEAL =====================\nconst revealEls = document.querySelectorAll('.reveal');\nconst reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;\nif (reduceMotion) {\n  revealEls.forEach(el => el.classList.add('is-visible'));\n} else {\n  const io = new IntersectionObserver((entries) => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        entry.target.classList.add('is-visible');\n        io.unobserve(entry.target);\n      }\n    });\n  }, { threshold: 0.15 });\n  revealEls.forEach(el => io.observe(el));\n}\n\n// ===================== COUNT UP (hero stats) =====================\nfunction countUp(el, target, duration = 1400) {\n  const start = performance.now();\n  function tick(now) {\n    const progress = Math.min((now - start) / duration, 1);\n    const eased = 1 - Math.pow(1 - progress, 3);\n    el.textContent = Math.floor(eased * target).toLocaleString('pt-BR');\n    if (progress < 1) requestAnimationFrame(tick);\n  }\n  requestAnimationFrame(tick);\n}\nconst heroDashEl = document.getElementById('heroDash');\nconst heroBars = document.getElementById('heroBars');\nlet heroAnimated = false;\nconst heroObserver = new IntersectionObserver((entries) => {\n  entries.forEach(entry => {\n    if (entry.isIntersecting && !heroAnimated) {\n      heroAnimated = true;\n      document.querySelectorAll('.dash-stat-value[data-count]').forEach(el => {\n        countUp(el, parseInt(el.dataset.count, 10));\n      });\n      heroBars.classList.add('in-view');\n      heroObserver.disconnect();\n    }\n  });\n}, { threshold: 0.3 });\nheroObserver.observe(heroDashEl);\n\n// ===================== HERO MOUSE PARALLAX (profundidade em camadas) =====================\nif (!reduceMotion) {\n  const heroSection = document.querySelector('.hero');\n  const heroContentEl = document.querySelector('.hero-content');\n  const heroGridEl = document.querySelector('.hero-grid');\n  let parallaxRaf = null;\n  let targetX = 0, targetY = 0, curX = 0, curY = 0;\n\n  heroSection.addEventListener('mousemove', (e) => {\n    const rect = heroSection.getBoundingClientRect();\n    targetX = ((e.clientX - rect.left) / rect.width - 0.5) * 2;\n    targetY = ((e.clientY - rect.top) / rect.height - 0.5) * 2;\n    if (!parallaxRaf) parallaxRaf = requestAnimationFrame(applyParallax);\n  });\n  heroSection.addEventListener('mouseleave', () => {\n    targetX = 0; targetY = 0;\n    if (!parallaxRaf) parallaxRaf = requestAnimationFrame(applyParallax);\n  });\n\n  function applyParallax() {\n    curX += (targetX - curX) * 0.08;\n    curY += (targetY - curY) * 0.08;\n\n    heroContentEl.style.transform = `translate3d(${curX * 6}px, ${curY * 4}px, 0)`;\n    heroGridEl.style.transform = `translate3d(${curX * -14}px, ${curY * -10}px, 0)`;\n    if (heroDashEl) {\n      heroDashEl.style.transform = `rotateX(${curY * -3}deg) rotateY(${curX * 4}deg)`;\n    }\n\n    if (Math.abs(targetX - curX) > 0.001 || Math.abs(targetY - curY) > 0.001) {\n      parallaxRaf = requestAnimationFrame(applyParallax);\n    } else {\n      parallaxRaf = null;\n    }\n  }\n}\n\n// ===================== MAGNETIC BUTTONS =====================\nif (!reduceMotion) {\n  document.querySelectorAll('.btn-magnetic').forEach((btn) => {\n    btn.addEventListener('mousemove', (e) => {\n      const rect = btn.getBoundingClientRect();\n      const x = e.clientX - rect.left - rect.width / 2;\n      const y = e.clientY - rect.top - rect.height / 2;\n      btn.style.transform = `translate(${x * 0.25}px, ${y * 0.35}px)`;\n    });\n    btn.addEventListener('mouseleave', () => { btn.style.transform = ''; });\n  });\n}\n\n// ===================== CARD TILT (Problemas, Recursos, Depoimentos, Planos) =====================\nif (!reduceMotion) {\n  document.querySelectorAll('.tilt-card').forEach((card) => {\n    card.addEventListener('mousemove', (e) => {\n      const rect = card.getBoundingClientRect();\n      const x = (e.clientX - rect.left) / rect.width - 0.5;\n      const y = (e.clientY - rect.top) / rect.height - 0.5;\n      card.style.transform = `perspective(700px) rotateX(${y * -4}deg) rotateY(${x * 5}deg) translateY(-2px)`;\n    });\n    card.addEventListener('mouseleave', () => {\n      card.style.transform = '';\n    });\n  });\n}\n\n// ===================== HERO CURSOR SPOTLIGHT =====================\nif (!reduceMotion) {\n  const heroSection = document.querySelector('.hero');\n  if (heroSection) {\n    const spotlight = document.createElement('div');\n    spotlight.className = 'hero-spotlight';\n    heroSection.appendChild(spotlight);\n    let rafId = null;\n    heroSection.addEventListener('mousemove', (e) => {\n      const rect = heroSection.getBoundingClientRect();\n      const x = e.clientX - rect.left;\n      const y = e.clientY - rect.top;\n      if (rafId) cancelAnimationFrame(rafId);\n      rafId = requestAnimationFrame(() => {\n        spotlight.style.transform = `translate(${x}px, ${y}px)`;\n        spotlight.style.opacity = '1';\n      });\n    });\n    heroSection.addEventListener('mouseleave', () => { spotlight.style.opacity = '0'; });\n  }\n}\n\n// ===================== HERO DASH TILT =====================\nif (!reduceMotion) {\n  const heroWrap = document.querySelector('.hero-dash-wrap');\n  heroWrap.addEventListener('mousemove', (e) => {\n    const rect = heroDashEl.getBoundingClientRect();\n    const x = (e.clientX - rect.left) / rect.width - 0.5;\n    const y = (e.clientY - rect.top) / rect.height - 0.5;\n    heroDashEl.style.transform = `rotateX(${y * -6}deg) rotateY(${x * 8}deg)`;\n  });\n  heroWrap.addEventListener('mouseleave', () => {\n    heroDashEl.style.transform = 'rotateX(0deg) rotateY(0deg)';\n  });\n}\n\n// ===================== MOCKUP TABS =====================\nconst mockTabs = document.querySelectorAll('.mock-tab');\nconst mockScreens = document.querySelectorAll('.mock-screen');\nmockTabs.forEach(tab => {\n  tab.addEventListener('click', () => {\n    mockTabs.forEach(t => t.classList.remove('active'));\n    mockScreens.forEach(s => s.classList.remove('active'));\n    tab.classList.add('active');\n    document.getElementById(tab.dataset.target).classList.add('active');\n  });\n});\n\n// ===================== FAQ ACCORDION =====================\ndocument.querySelectorAll('.faq-item').forEach(item => {\n  const btn = item.querySelector('.faq-q');\n  const ans = item.querySelector('.faq-a');\n  btn.addEventListener('click', () => {\n    const isOpen = item.classList.contains('open');\n    document.querySelectorAll('.faq-item.open').forEach(openItem => {\n      if (openItem !== item) {\n        openItem.classList.remove('open');\n        openItem.querySelector('.faq-q').setAttribute('aria-expanded', 'false');\n        openItem.querySelector('.faq-a').style.maxHeight = null;\n      }\n    });\n    if (isOpen) {\n      item.classList.remove('open');\n      btn.setAttribute('aria-expanded', 'false');\n      ans.style.maxHeight = null;\n    } else {\n      item.classList.add('open');\n      btn.setAttribute('aria-expanded', 'true');\n      ans.style.maxHeight = ans.scrollHeight + 'px';\n    }\n  });\n});\n\n// ===================== SIGNUP MODAL =====================\nconst modalOverlay = document.getElementById('modalOverlay');\nconst modalPlanName = document.getElementById('modalPlanName');\nconst modalClose = document.getElementById('modalClose');\n\nfunction openModal(planName) {\n  modalPlanName.textContent = planName;\n  modalOverlay.classList.add('open');\n  document.body.style.overflow = 'hidden';\n}\nfunction closeModal() {\n  modalOverlay.classList.remove('open');\n  document.body.style.overflow = '';\n}\nmodalClose.addEventListener('click', closeModal);\nmodalOverlay.addEventListener('click', (e) => { if (e.target === modalOverlay) closeModal(); });\ndocument.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); });\n\n// ===================== FULL-PAGE PARTICLE SYSTEM (intensidade por seção) =====================\n(function () {\n  const canvas = document.getElementById('heroCanvas');\n  if (!canvas) return;\n  const ctx = canvas.getContext('2d');\n  let w, h, particles = [], backParticles = [];\n  const mouse = { x: -9999, y: -9999 };\n  const COUNT = reduceMotion ? 0 : (window.innerWidth < 700 ? 32 : 65);\n  const BACK_COUNT = reduceMotion ? 0 : (window.innerWidth < 700 ? 14 : 26);\n  let sectionIntensity = 1;\n\n  function resize() {\n    w = canvas.width = window.innerWidth;\n    h = canvas.height = window.innerHeight;\n  }\n  function makeParticles() {\n    particles = [];\n    for (let i = 0; i < COUNT; i++) {\n      particles.push({\n        x: Math.random() * w, y: Math.random() * h,\n        vx: (Math.random() - 0.5) * 0.22, vy: (Math.random() - 0.5) * 0.22,\n        r: Math.random() * 1.5 + 0.6, gold: Math.random() > 0.78,\n      });\n    }\n    backParticles = [];\n    for (let i = 0; i < BACK_COUNT; i++) {\n      backParticles.push({\n        x: Math.random() * w, y: Math.random() * h,\n        vx: (Math.random() - 0.5) * 0.09, vy: (Math.random() - 0.5) * 0.09,\n        r: Math.random() * 2.6 + 2, gold: Math.random() > 0.7,\n      });\n    }\n  }\n  function step() {\n    ctx.clearRect(0, 0, w, h);\n    ctx.globalAlpha = sectionIntensity;\n    const isLight = document.documentElement.getAttribute('data-theme') === 'light';\n    const blueDot = isLight ? 'rgba(37,99,235,0.55)' : 'rgba(91,141,239,0.85)';\n    const goldDot = isLight ? 'rgba(180,130,10,0.55)' : 'rgba(251,191,36,0.85)';\n    const blueGlow = isLight ? 'rgba(37,99,235,0.35)' : 'rgba(37,99,235,0.8)';\n    const goldGlow = isLight ? 'rgba(180,130,10,0.35)' : 'rgba(251,191,36,0.8)';\n    const blueDotBack = isLight ? 'rgba(37,99,235,0.22)' : 'rgba(91,141,239,0.28)';\n    const goldDotBack = isLight ? 'rgba(180,130,10,0.2)' : 'rgba(251,191,36,0.28)';\n    const lineBase = isLight ? '37,99,235' : '91,141,239';\n    const lineMax = isLight ? 0.13 : 0.16;\n    const shadowAmt = isLight ? 3 : 6;\n    const shadowAmtBack = isLight ? 7 : 14;\n\n    // back depth layer — larger, slower, blurrier, barely reacts to mouse (feels distant)\n    for (const p of backParticles) {\n      const dx = p.x - mouse.x, dy = p.y - mouse.y;\n      const dist2 = dx * dx + dy * dy;\n      if (dist2 < 16000) {\n        const dist = Math.sqrt(dist2) || 1;\n        const force = (126 - dist) / 126;\n        p.x += (dx / dist) * force * 0.25;\n        p.y += (dy / dist) * force * 0.25;\n      }\n      p.x += p.vx; p.y += p.vy;\n      if (p.x < -20) p.x = w + 20; if (p.x > w + 20) p.x = -20;\n      if (p.y < -20) p.y = h + 20; if (p.y > h + 20) p.y = -20;\n      ctx.beginPath();\n      ctx.fillStyle = p.gold ? goldDotBack : blueDotBack;\n      ctx.shadowColor = p.gold ? goldGlow : blueGlow;\n      ctx.shadowBlur = shadowAmtBack;\n      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);\n      ctx.fill();\n    }\n\n    // front layer — sharper, faster, fully mouse-reactive\n    for (const p of particles) {\n      const dx = p.x - mouse.x, dy = p.y - mouse.y;\n      const dist2 = dx * dx + dy * dy;\n      if (dist2 < 14400) {\n        const dist = Math.sqrt(dist2) || 1;\n        const force = (120 - dist) / 120;\n        p.x += (dx / dist) * force * 1.1;\n        p.y += (dy / dist) * force * 1.1;\n      }\n      p.x += p.vx; p.y += p.vy;\n      if (p.x < -10) p.x = w + 10; if (p.x > w + 10) p.x = -10;\n      if (p.y < -10) p.y = h + 10; if (p.y > h + 10) p.y = -10;\n    }\n    for (let i = 0; i < particles.length; i++) {\n      for (let j = i + 1; j < particles.length; j++) {\n        const a = particles[i], b = particles[j];\n        const dx = a.x - b.x, dy = a.y - b.y;\n        const dist2 = dx * dx + dy * dy;\n        if (dist2 < 15000) {\n          ctx.strokeStyle = `rgba(${lineBase},${((1 - dist2 / 15000) * lineMax).toFixed(3)})`;\n          ctx.lineWidth = 1;\n          ctx.beginPath(); ctx.moveTo(a.x, a.y); ctx.lineTo(b.x, b.y); ctx.stroke();\n        }\n      }\n    }\n    for (const p of particles) {\n      ctx.beginPath();\n      ctx.fillStyle = p.gold ? goldDot : blueDot;\n      ctx.shadowColor = p.gold ? goldGlow : blueGlow;\n      ctx.shadowBlur = shadowAmt;\n      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);\n      ctx.fill();\n    }\n    ctx.shadowBlur = 0;\n    ctx.globalAlpha = 1;\n    requestAnimationFrame(step);\n  }\n  window.addEventListener('resize', resize, { passive: true });\n  document.addEventListener('mousemove', (e) => { mouse.x = e.clientX; mouse.y = e.clientY; });\n  document.addEventListener('mouseleave', () => { mouse.x = -9999; mouse.y = -9999; });\n\n  // intensity by section: 100% hero, 60% features, 40% pricing, 20% footer\n  function updateIntensity() {\n    const checkpoints = [\n      { y: 0, v: 1 },\n      { y: (document.querySelector('.hero')?.getBoundingClientRect().bottom ?? 0) + window.scrollY, v: 1 },\n      { y: (document.getElementById('features')?.getBoundingClientRect().top ?? 0) + window.scrollY, v: 0.6 },\n      { y: (document.getElementById('pricing')?.getBoundingClientRect().top ?? 0) + window.scrollY, v: 0.4 },\n      { y: (document.querySelector('footer')?.getBoundingClientRect().top ?? 0) + window.scrollY, v: 0.2 },\n    ];\n    const mid = window.scrollY + window.innerHeight * 0.4;\n    let result = checkpoints[0].v;\n    for (let i = 0; i < checkpoints.length - 1; i++) {\n      const a = checkpoints[i], b = checkpoints[i + 1];\n      if (mid >= a.y && mid <= b.y && b.y > a.y) {\n        const t = (mid - a.y) / (b.y - a.y);\n        result = a.v + (b.v - a.v) * t;\n        break;\n      }\n      if (mid > b.y) result = b.v;\n    }\n    sectionIntensity = Math.max(0.2, Math.min(1, result));\n  }\n  window.addEventListener('scroll', updateIntensity, { passive: true });\n  updateIntensity();\n  resize();\n  makeParticles();\n  if (!reduceMotion) requestAnimationFrame(step);\n})();\n\n// ===================== MAGNETIC BUTTONS =====================\nif (!reduceMotion) {\n  document.querySelectorAll('.btn-primary, .btn-gold').forEach((btn) => {\n    btn.addEventListener('mousemove', (e) => {\n      const r = btn.getBoundingClientRect();\n      const x = e.clientX - r.left - r.width / 2;\n      const y = e.clientY - r.top - r.height / 2;\n      btn.style.transform = `translate(${x * 0.18}px, ${y * 0.35}px)`;\n    });\n    btn.addEventListener('mouseleave', () => { btn.style.transform = ''; });\n  });\n}\n\n// ===================== SCROLL STORYTELLING ENGINE =====================\n(function () {\n  const story = document.getElementById('story');\n  if (!story) return;\n  const stages = Array.from(document.querySelectorAll('.story-stage'));\n  const dots = Array.from(document.querySelectorAll('.story-dot'));\n  const caption = document.getElementById('storyCaption');\n  const names = ['Dashboard', 'Recrutamento', 'Onboarding & Documentos', 'Portal do Colaborador', 'IA de Documentos'];\n  let ticking = false;\n  let lastActive = -1;\n  let iaPlayed = false;\n\n  function render() {\n    ticking = false;\n    const rect = story.getBoundingClientRect();\n    const total = rect.height - window.innerHeight;\n    const progress = Math.min(1, Math.max(0, -rect.top / Math.max(1, total)));\n    const scaled = progress * stages.length;\n    let active = Math.min(stages.length - 1, Math.floor(scaled));\n    let local = scaled - active;\n    if (progress >= 1) { active = stages.length - 1; local = 0; }\n    if (progress <= 0) { active = 0; local = 0; }\n\n    stages.forEach((stage, i) => {\n      let opacity = 0, transform = 'translateY(24px) scale(.94)', blur = 0;\n      if (i === active) {\n        const fadeT = local > 0.82 ? (local - 0.82) / 0.18 : 0;\n        opacity = 1 - fadeT;\n        transform = 'translateY(0) scale(1)';\n        blur = fadeT * 3;\n        if (i === 0 && active === 0 && stages.length > 1) {\n          const rot = local * 10;\n          transform = `translateY(0) scale(${1 - local * 0.06}) rotateY(${-rot}deg)`;\n          blur = Math.max(blur, fadeT * 4);\n        }\n      } else if (i === active + 1 && local > 0.82) {\n        const t = (local - 0.82) / 0.18;\n        opacity = t;\n        transform = `translateY(${(1 - t) * 24}px) scale(${0.94 + t * 0.06})`;\n        blur = (1 - t) * 3;\n      }\n      stage.style.opacity = opacity;\n      stage.style.transform = transform;\n      stage.style.filter = reduceMotion || blur < 0.15 ? '' : `blur(${blur.toFixed(2)}px)`;\n      stage.style.pointerEvents = opacity > 0.5 ? 'auto' : 'none';\n    });\n\n    const captionIndex = local > 0.9 && active < stages.length - 1 ? active + 1 : active;\n    if (captionIndex !== lastActive) {\n      lastActive = captionIndex;\n      caption.textContent = `0${captionIndex + 1} — ${names[captionIndex]}`;\n      caption.classList.add('show');\n      dots.forEach((d, i) => d.classList.toggle('active', i === captionIndex));\n      stages.forEach((s, i) => s.classList.toggle('in', i === captionIndex));\n      if (captionIndex === 4 && !iaPlayed) { iaPlayed = true; playIaSequence(); }\n      if (captionIndex === 4 && iaPlayed) { restartIaLoop(); }\n    }\n  }\n  function onScroll() {\n    if (!ticking) { ticking = true; requestAnimationFrame(render); }\n  }\n  window.addEventListener('scroll', onScroll, { passive: true });\n  window.addEventListener('resize', onScroll, { passive: true });\n  render();\n\n  // IA de Documentos — protagonist autoplay sequence\n  let iaTimers = [];\n  function clearIaTimers() { iaTimers.forEach(clearTimeout); iaTimers = []; }\n  function playIaSequence() {\n    clearIaTimers();\n    const frame = document.getElementById('iaDocFrame');\n    const scan = document.getElementById('iaScanLine');\n    const fields = document.getElementById('iaFields');\n    const badge = document.getElementById('iaBadge');\n    const saved = document.getElementById('iaSaved');\n    if (!frame) return;\n    frame.classList.remove('corrected'); scan.classList.remove('run');\n    fields.classList.remove('show'); badge.classList.remove('show'); saved.classList.remove('show');\n    const delay = reduceMotion ? 60 : 700;\n    iaTimers.push(setTimeout(() => frame.classList.add('corrected'), delay));\n    iaTimers.push(setTimeout(() => scan.classList.add('run'), delay * 1.6));\n    iaTimers.push(setTimeout(() => fields.classList.add('show'), delay * 3.1));\n    iaTimers.push(setTimeout(() => badge.classList.add('show'), delay * 3.8));\n    iaTimers.push(setTimeout(() => saved.classList.add('show'), delay * 4.4));\n    iaTimers.push(setTimeout(() => { if (lastActive === 4) playIaSequence(); }, delay * 7));\n  }\n  function restartIaLoop() { /* sequence re-triggers itself via the trailing timer above */ }\n})();\n\n// ===================== PREMIUM CHART: draw-in + hover tooltip =====================\n(function () {\n  const svg = document.getElementById('storyChartSvg');\n  if (!svg) return;\n  const path = document.getElementById('chartCrispLine');\n  const glowPath = document.getElementById('chartGlowLine');\n  const guide = document.getElementById('chartGuide');\n  const hoverDot = document.getElementById('chartHoverDot');\n  const tooltip = document.getElementById('chartTooltip');\n\n  const len = path.getTotalLength();\n  [path, glowPath].forEach((p) => {\n    p.style.strokeDasharray = len;\n    p.style.strokeDashoffset = reduceMotion ? 0 : len;\n  });\n  let drawn = false;\n  function drawIn() {\n    if (drawn) return;\n    drawn = true;\n    [path, glowPath].forEach((p) => {\n      p.style.transition = 'stroke-dashoffset 1.4s cubic-bezier(.22,1,.36,1)';\n      requestAnimationFrame(() => { p.style.strokeDashoffset = 0; });\n    });\n  }\n  const dashboardStage = document.querySelector('.story-stage[data-stage=\"dashboard\"]');\n  if (dashboardStage) {\n    const chartIo = new IntersectionObserver((entries) => {\n      entries.forEach((e) => { if (e.isIntersecting) { drawIn(); chartIo.disconnect(); } });\n    }, { threshold: 0.15 });\n    chartIo.observe(dashboardStage);\n  }\n\n  const months = [\n    { x: 0, label: 'Jul', value: 1080 }, { x: 100, label: 'Ago', value: 1102 },\n    { x: 200, label: 'Set', value: 1118 }, { x: 300, label: 'Out', value: 1150 },\n    { x: 400, label: 'Nov', value: 1178 }, { x: 500, label: 'Dez', value: 1215 },\n    { x: 600, label: 'Jan', value: 1248 },\n  ];\n  function yAtX(targetX) {\n    let lo = 0, hi = len, best = { x: 0, y: 0 };\n    for (let i = 0; i <= 30; i++) {\n      const mid = (lo + hi) / 2;\n      const pt = path.getPointAtLength(mid);\n      best = pt;\n      if (Math.abs(pt.x - targetX) < 0.4) break;\n      if (pt.x < targetX) lo = mid; else hi = mid;\n    }\n    return best.y;\n  }\n  const markers = months.map((m) => ({ ...m, y: yAtX(m.x) }));\n\n  svg.addEventListener('mousemove', (e) => {\n    const rect = svg.getBoundingClientRect();\n    const mx = ((e.clientX - rect.left) / rect.width) * 600;\n    let nearest = markers[0];\n    markers.forEach((m) => { if (Math.abs(m.x - mx) < Math.abs(nearest.x - mx)) nearest = m; });\n    hoverDot.setAttribute('cx', nearest.x);\n    hoverDot.setAttribute('cy', nearest.y);\n    hoverDot.style.opacity = 1;\n    guide.setAttribute('x1', nearest.x);\n    guide.setAttribute('x2', nearest.x);\n    guide.style.opacity = 1;\n    tooltip.style.left = (nearest.x / 600 * 100) + '%';\n    tooltip.style.top = (nearest.y / 160 * 100) + '%';\n    tooltip.textContent = `${nearest.label} — ${nearest.value.toLocaleString('pt-BR')} colaboradores`;\n    tooltip.classList.add('show');\n  });\n  svg.addEventListener('mouseleave', () => {\n    hoverDot.style.opacity = 0;\n    guide.style.opacity = 0;\n    tooltip.classList.remove('show');\n  });\n})();\n\n// ===================== HERO MULTI-LAYER SCROLL PARALLAX =====================\n(function () {\n  const heroEl = document.querySelector('.hero');\n  const grid = document.querySelector('.hero-grid');\n  const canvasEl = document.getElementById('heroCanvas');\n  if (!heroEl || reduceMotion) return;\n  let ticking = false;\n  function applyParallax() {\n    ticking = false;\n    const rect = heroEl.getBoundingClientRect();\n    if (rect.bottom < 0 || rect.top > window.innerHeight) return;\n    const offset = -rect.top;\n    if (grid) grid.style.transform = `translateY(${offset * 0.08}px)`;\n    if (canvasEl) canvasEl.style.transform = `translateY(${offset * 0.18}px)`;\n  }\n  window.addEventListener('scroll', () => { if (!ticking) { ticking = true; requestAnimationFrame(applyParallax); } }, { passive: true });\n  applyParallax();\n})();\n\n// ===================== BUTTON RIPPLE (microinteração) =====================\nif (!reduceMotion) {\n  document.addEventListener('click', (e) => {\n    const btn = e.target.closest('.btn');\n    if (!btn) return;\n    const rect = btn.getBoundingClientRect();\n    const ripple = document.createElement('span');\n    ripple.className = 'btn-ripple';\n    const size = Math.max(rect.width, rect.height) * 1.6;\n    ripple.style.width = ripple.style.height = size + 'px';\n    ripple.style.left = (e.clientX - rect.left - size / 2) + 'px';\n    ripple.style.top = (e.clientY - rect.top - size / 2) + 'px';\n    btn.appendChild(ripple);\n    setTimeout(() => ripple.remove(), 650);\n  });\n}\n";
